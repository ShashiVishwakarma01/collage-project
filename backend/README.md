# Portfolio Backend

Spring Boot REST API generated in the style of Spring Initializr.

## Run

From this directory:

```powershell
mvnw.cmd spring-boot:run
```

The API is available at `http://localhost:8080/api/portfolio`.
Health is available at `http://localhost:8080/actuator/health`.
